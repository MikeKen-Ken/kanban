{
  "name": "kanban-agent-dispatch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "本机 Cursor SDK / Codex exec worker，由看板 App 调度",
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --format=esm --packages=external --outfile=dist/cli.js",
    "start": "node dist/cli.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.0",
    "@openai/codex": "^0.147.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
