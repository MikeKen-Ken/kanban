# 领域词汇

- **工作区（Workspace）**：一次同步所需的完整用户内容集合，包括项目清单、项目数据和应用级共享数据。
- **项目（Project）**：一个独立看板及其设置、回收站、模板、保存视图和活动历史。
- **看板（Board）**：项目内按顺序排列的列集合。
- **列（Column）**：承载卡片的工作流阶段，可配置排序、置顶和 WIP 上限。
- **卡片（Card）**：任务实体，包含标题、备注、状态、日期、优先级、标签、子任务和附件。
- **已完成列（Done Column）**：项目设置指定的完成阶段。卡片勾选完成时会移入该列。
- **日历视图（Calendar View）**：按到期日浏览与调整跨项目未完成卡片；选中某日可在底部查看当日任务（含今日）。MCP `today_cards` 仍提供逾期/今天/本周聚合查询。
- **保存视图（Saved View）**：用户命名并同步的组合筛选条件。
- **快速录入（Quick Capture）**：无需进入具体列即可创建卡片的入口。
- **重复系列（Recurrence Series）**：由同一重复规则生成的一组卡片；系列标识与周期键用于多设备幂等。
- **提醒（Reminder）**：同步的提醒时刻和本机系统通知调度的组合。
- **WIP 上限**：列内未完成卡片的建议最大数量。当前产品采用软限制，只警告和高亮，不阻止操作。
- **模板（Template）**：可复用的卡片字段集合，不包含图片二进制。
- **活动事件（Activity Event）**：项目内追加且不可修改的用户操作记录。
- **回收站（Trash）**：可恢复的删除快照；普通删除和冲突确认删除都应进入回收站。
- **SyncBase**：本机保存的上次成功合并工作区，用于 local/base/remote 三路合并。
- **冲突（Conflict）**：同一字段或删改意图无法自动合并时保留的两侧结果。
- **共享数据（Shared Data）**：跨项目且通过 WebDAV 同步的自定义标签等用户内容。
- **本机偏好（Local Preferences）**：不参与 WebDAV 同步的界面和设备设置。
